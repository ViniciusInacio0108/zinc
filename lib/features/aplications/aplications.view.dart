import 'package:flutter/material.dart';

class AplicationView extends StatelessWidget {
  const AplicationView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
